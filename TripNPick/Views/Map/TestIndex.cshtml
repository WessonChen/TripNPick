@model TripNPick.Models.UserSelections
@{
    ViewBag.Title = "TestIndex";
}

<head>
    <link rel="stylesheet" type="text/css" href="~/Content/maps.css" />
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/dt/dt-1.10.16/datatables.min.css" />
    <script type="text/javascript" src="https://cdn.datatables.net/v/dt/dt-1.10.16/datatables.min.js"></script>
    <script src="~/Scripts/mapTags.js" type="text/javascript"></script>
</head>

<script type="text/javascript">
    function initMap() {

        var map = new google.maps.Map(document.getElementById('testMap'), {
            zoom: 3.9,
            center: { lat: -25.031596581410874, lng: 134.36777208750004 }
        });

        var markersVIC = [];
        var markersNSW = [];
        var markersSA = [];
        var markersWA = [];
        var markersTAS = [];
        var markersQLD = [];
        var markersNT = [];

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":VIC"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersVIC.push(marker);
            }
            var markersVICCluster = new MarkerClusterer(map, markersVIC,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":NSW"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersNSW.push(marker);
            }
            var markersNSWCluster = new MarkerClusterer(map, markersNSW,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":SA"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersSA.push(marker);
            }
            var markersSACluster = new MarkerClusterer(map, markersSA,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":WA"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersWA.push(marker);
            }
            var markersWACluster = new MarkerClusterer(map, markersWA,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":TAS"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersTAS.push(marker);
            }
            var markersTASCluster = new MarkerClusterer(map, markersTAS,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":QLD"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersQLD.push(marker);
            }
            var markersQLDCluster = new MarkerClusterer(map, markersQLD,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })

        $.get("@Url.Action("doTheDew", "Map", new { userInput = Model.combinedString + ":NT"})", function (data, status) {
            for (i = 0; i < data.length; i++) {
                marker = new google.maps.Marker({
                    position: new google.maps.LatLng(data[i].farm.farm_lat, data[i].farm.farm_lng),
                    map: map
                });
                markersNT.push(marker);
            }
            var markersNTCluster = new MarkerClusterer(map, markersNT,
                { imagePath: 'https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/m' });
        })
    }
</script>
<script src="https://developers.google.com/maps/documentation/javascript/examples/markerclusterer/markerclusterer.js"></script>
<script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAdNjpD2meHBBiquEK62khiotrQ_4u5WpQ&callback=initMap"></script>

<body>
    <div class="container bgBorder">
        <div style="margin-left: 27px">
            @Html.MvcSiteMap().SiteMapPath()
        </div>

        <form action="/Map/TestIndex" method="post">

        </form>

        <div style="margin-top: 16px">
            <div class="col-lg-3">
            </div>

            <div id="testMap" class="col-lg-9" style="height: 600px">
            </div>
        </div>
    </div>
</body>
